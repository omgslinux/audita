{% extends 'base.html.twig' %}

{% block title %}BudgetChapters index{% endblock %}

{% block body %}
    <h1>BudgetChapters index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Code</th>
                <th>Description</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for budget_chapter in budget_chapters %}
            <tr>
                <td>{{ budget_chapter.id }}</td>
                <td>{{ budget_chapter.code }}</td>
                <td>{{ budget_chapter.description }}</td>
                <td>
                    <a href="{{ path(PREFIX ~ 'show', {'id': budget_chapter.id}) }}">show</a>
                    <a href="{{ path(PREFIX ~ 'edit', {'id': budget_chapter.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path(PREFIX ~ 'new') }}">Create new</a>
{% endblock %}
